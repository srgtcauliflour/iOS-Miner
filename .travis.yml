os: osx

osx_image: xcode9.2

compiler:
  - clang
  
notifications:
  email: false
  
language: c

before_install:
  - brew update && brew bundle
  - sudo ./install-curl.sh

before_script:
  - ./autogen.sh
  - ./nomacro.pl

script:
  - ./configure
  - make


sudo: true
